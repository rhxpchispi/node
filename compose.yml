services:
  myapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myapp6
    image: rhxpchispi/myapp:6.0
    ports:
      - 3030:3000
    networks:
      - myapp-network
networks:
  myapp-network:
    driver: bridge
